# Session handovers

Task-scoped context for session continuity. **Not** the APP execution standard.

Read a file here **only when directed** (explicit path or @-mention). Do not browse or search this folder proactively. When a handover conflicts with [`standard/`](../../standard/) or the working tree, follow `standard/` and the tree.
